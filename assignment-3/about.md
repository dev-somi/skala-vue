**과제 요구사항**

**1. 배열 렌더링 (v-for)**

- 임의의 날씨 데이터 배열을 활용해 화면에 날씨 카드를 반복 출력한다.

```javascript
const weatherList = ref([
  { id: 'city_01', name: '서울', temp: 28, status: '맑음' },
  { id: 'city_02', name: '수원', temp: 24, status: '비' },
  { id: 'city_03', name: '부산', temp: 26, status: '구름' },
])
```

- `:key`에 id 바인딩 필수

**2. 조건부 렌더링 (v-if)**

- 기온이 25도 이상인 도시는 "🩸 더움 (25도 이상)", 25도 미만인 도시는 "❄️ 선선함 (25도 미만)" 라벨을 붙인다. (조건은 다르게 해도 된다.)

**3. 양방향 바인딩 및 한글 처리 (:value, @input)**

- 도시 이름을 한글로 검색하는 input을 만든 후 한글 입력 후 입력 한 도시명을 출력한다.

**4. 이벤트 및 수식어**

- 지역별 날씨 현황 카드를 누르면 상태바에 “{도시}이 선택되었습니다.” 표기
- 지역별 날씨 현황 카드 내부의 [상세보기] 버튼을 누르면 **버블링 없이** 해당 도시의 날씨 내용을 window.alert로 띄운다.

```javascript
const showDetail = (cityName, status) => {
  window.alert(`${cityName}의 현재 날씨는 [${status}] 상태입니다.`)
}
```

**5. 본인만의 데이터를 추가하고 이를 기초로 Mockup을 추가한다.**

이 과에서 takeaway 한것: vue directive 사용법 <- 이거 템플릿에만 적용이 된다는거
그리고 조건부 렌더링 특히 v-if와 v-show의 차이 및 어떤 상황에 어떤걸 써야되는지
(이 과제에서 조건부 렌더링은 조건이 애매해서 최종과제에서는 제외함)

v-bind에 대한 이해
vue.js가 선언적렌더링이여서 template에서 binding만 해주면 그 직접 값을 주입을 안해줘도
script에 작성이되어있으면 알아서 적용을 해주는거
또한 양방향 바인딩은 단방향 바인딩이랑 input을 동시에 해놓은 sugar 문법이라는점
암튼 그래서 양방향 바인딩을 통해서 사용자에게 입력받은 것을 실시간으로 화면에 띄워줄 수가 있음

그리고 버블링, 타겟, 캡처링에 대한 이해
어떤 이벤트가 발생했을때 그 객체/요소에만 딱 발생을 하는게 아니라
이제 부모에서 시작해서 자식을 타고가고 (이게 캡처링) 이제 다시 자식에서 부모로 올라간다는거 (이게 버블링)
그래서 이것도 template modifier? vue의 기능을 이용해서 간단하게 제어할 수 있다는 점
